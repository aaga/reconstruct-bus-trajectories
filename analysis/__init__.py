"""Analysis layer: results, prep helpers, and dashboard-payload builders.

Importable via the repo root being on the path (``pythonpath`` for tests; the
data-build scripts add ``REPO`` to ``sys.path``). Depends on the core library
and ``dataio``; never imported by ``core``.
"""
